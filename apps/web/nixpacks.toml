[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd ../.. && npm install"]

[phases.build]
cmds = ["cd ../.. && npm run build --workspace=@buildkit/web"]

[start]
cmd = "npx serve dist -s -l 3000"
